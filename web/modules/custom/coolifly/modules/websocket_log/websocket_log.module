<?php

/**
 * @file
 * Websocket log hooks.
 */

/**
 * Implements hook_theme().
 */
function websocket_log_theme($existing, $type, $theme, $path) {
  return [
    'log_block' => [
      'variables' => [
        'markup' => '',
      ],
      'template' => 'block--log-block',
    ],
  ];
}
